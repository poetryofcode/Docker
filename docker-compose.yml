services:
  app:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/app
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: mongo
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
