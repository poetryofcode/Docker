version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: mongo
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
